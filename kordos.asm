; Definitions

; Includes

; Interrupt table

; Init
    ; Set timer for interrupt
    ; set default stack values for tasks
    ; set 

; Time ticker
    TimerService
; Slow Interrupt handler
    IntService     
; Programm loader (loop)
    TaskLoad

; Timer set system call

; waitForInt system call
